//#define DEBUG
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef vector<int> vi;
typedef vector<ll> vl;
typedef vector<vi> vvi;



int solve() {
    int n, ret;
    cin >> n;

    for (int i = 0; i < n; i++) {
        cin >> ;
    }


    return ret;
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0); // remove this line for interactive problems
#ifdef DEBUG
    freopen("input.txt", "r", stdin);
#endif
    int t = 1;
    // cin >> t;
    for (int i = 0; i < t; i++) {
        cout << solve() << endl;
    }
}
